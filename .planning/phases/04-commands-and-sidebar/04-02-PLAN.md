---
phase: 04-commands-and-sidebar
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/sidebar.ts
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "Sidebar panel correctly classifies folders into two groups: those with _self-test.md and those without"
    - "Only folders with at least one non-_self-test .md file appear (vault root excluded)"
    - "Last-generated date is derived from TFile.stat.mtime and formatted via toLocaleDateString()"
    - "All 10 sidebar unit tests pass (green)"
  artifacts:
    - path: "src/sidebar.ts"
      provides: "ActiveRecallSidebarView, getFolderStatuses, getLastGeneratedDate, buildContextMenuHandler, buildActivateView"
      exports: ["VIEW_TYPE_ACTIVE_RECALL", "ActiveRecallSidebarView", "getFolderStatuses", "getLastGeneratedDate", "buildContextMenuHandler", "buildActivateView"]
    - path: "src/__tests__/sidebar.test.ts"
      provides: "All 10 tests green"
      contains: "PASS"
  key_links:
    - from: "src/sidebar.ts"
      to: "src/generation.ts"
      via: "GenerationService import"
      pattern: "import.*GenerationService.*generation"
    - from: "src/sidebar.ts"
      to: "obsidian"
      via: "ItemView, TFile, TFolder imports"
      pattern: "import.*ItemView.*obsidian"
---

<objective>
Implement src/sidebar.ts - the ActiveRecallSidebarView ItemView subclass plus pure helper functions exported for testability. Make all 10 sidebar unit tests pass (green).

Purpose: Delivers UI-01 and UI-02 - the core sidebar data logic and panel rendering. The ItemView is implemented but not yet wired into main.ts (that happens in Plan 03).
Output: src/sidebar.ts with full implementation; npx jest green on sidebar tests
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-commands-and-sidebar/04-CONTEXT.md
@.planning/phases/04-commands-and-sidebar/04-RESEARCH.md
@.planning/phases/04-commands-and-sidebar/04-01-SUMMARY.md

<interfaces>
<!-- From src/generation.ts - GenerationService to be injected -->
export class GenerationService {
    constructor(app: App, settings: ActiveRecallSettings, statusBarItem: HTMLElement)
    async generate(folderPath: string): Promise<void>
}

<!-- From src/__mocks__/obsidian.ts (extended in Plan 01) -->
- TFile: { basename, extension, path, stat: { mtime: number } }
- TFolder: { children: (TFile|TFolder)[], path }
- WorkspaceLeaf, Menu, MenuItem (new in Plan 01)
- createMockApp(): { vault: { getAllFolders, ... }, workspace: { getLeavesOfType, getRightLeaf, revealLeaf } }

<!-- Obsidian API patterns from research -->
// ItemView base class:
export class ActiveRecallSidebarView extends ItemView {
    getViewType(): string
    getDisplayText(): string
    getIcon(): string
    async onOpen(): Promise<void>
    async onClose(): Promise<void>
    refresh(): void
    private renderPanel(): void
}

// VIEW_TYPE constant:
export const VIEW_TYPE_ACTIVE_RECALL = 'active-recall-panel';

// Folder enumeration:
// app.vault.getAllFolders(false) - excludes vault root
// Filter: folder.children.some(child => child instanceof TFile && child.extension === 'md' && child.basename !== '_self-test')
// Self-test detection: folder.children.find(child => child instanceof TFile && child.basename === '_self-test')

// Last-generated date:
// new Date(selfTestFile.stat.mtime).toLocaleDateString()

// activateView pattern (exported as buildActivateView for testability):
// 1. getLeavesOfType(VIEW_TYPE_ACTIVE_RECALL) - if length > 0, revealLeaf existing[0]
// 2. else: getRightLeaf(false) -> setViewState({ type, active: true }) -> revealLeaf

// context menu handler (exported as buildContextMenuHandler for testability):
// if (!(file instanceof TFolder)) return
// menu.addItem(item => item.setTitle('Generate Self-Test').setIcon('brain-circuit').onClick(...))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sidebar.ts pure helper functions (make unit tests green)</name>
  <files>src/sidebar.ts</files>
  <behavior>
    - getFolderStatuses(app): filters getAllFolders(false) to folders with >=1 non-_self-test .md file; maps each to { folder, selfTestFile: TFile|null }
    - getLastGeneratedDate(file): returns new Date(file.stat.mtime).toLocaleDateString()
    - buildActivateView(app, viewType): returns async function; when getLeavesOfType returns existing leaf, calls revealLeaf(existing[0]); when empty, calls getRightLeaf then setViewState then revealLeaf
    - buildContextMenuHandler(generationService, app, viewType): returns handler function (menu, file) => void; guards with instanceof TFolder; adds menu item with title "Generate Self-Test", icon "brain-circuit"; onClick calls generate then refreshes sidebar if open
  </behavior>
  <action>
Create src/sidebar.ts with these exports in this order:

1. Imports: `ItemView`, `WorkspaceLeaf`, `App`, `TFile`, `TFolder` from `obsidian`. Import `GenerationService` from `./generation`.

2. `export const VIEW_TYPE_ACTIVE_RECALL = 'active-recall-panel';`

3. `export interface FolderStatus { folder: TFolder; selfTestFile: TFile | null; }`

4. `export function getFolderStatuses(app: App): FolderStatus[]` - implementation per research Pattern 3 and Pitfall 5. Use `app.vault.getAllFolders(false)`, filter children, find selfTestFile.

5. `export function getLastGeneratedDate(file: TFile): string` - `return new Date(file.stat.mtime).toLocaleDateString();`

6. `export function buildActivateView(app: App, viewType: string): () => Promise<void>` - returns async arrow function implementing Pattern 1 from research. Checks getLeavesOfType first.

7. `export function buildContextMenuHandler(generationService: GenerationService, app: App, viewType: string): (menu: import('obsidian').Menu, file: import('obsidian').TAbstractFile) => void` - implements Pattern 2 from research. After generate resolves, calls refresh() on the sidebar view if found via getLeavesOfType.

8. `export class ActiveRecallSidebarView extends ItemView` - implement full class per research Pattern 6:
   - constructor(leaf: WorkspaceLeaf, private _app: App, private generationService: GenerationService)
   - getViewType(): return VIEW_TYPE_ACTIVE_RECALL
   - getDisplayText(): return 'Active Recall'
   - getIcon(): return 'brain-circuit'
   - async onOpen(): calls this.refresh()
   - async onClose(): this.contentEl.empty()
   - public refresh(): this.contentEl.empty(); this.renderPanel()
   - private renderPanel(): enumerate getFolderStatuses(this._app), split into withSelfTest and without, render using createEl/createDiv per Pattern 6. Sections hidden if empty (no h4 rendered). Buttons call this.onGenerate(folder.path).
   - private async onGenerate(folderPath: string): await this.generationService.generate(folderPath); this.refresh();

Note: Use `this._app` for the App reference (not `this.app`) to avoid collision with the inherited `app` property from ItemView which may not be correctly typed in all Obsidian versions.

Do NOT use innerHTML anywhere. Use contentEl.createEl(), createDiv(), createSpan() throughout.
  </action>
  <verify>
    <automated>cd /Users/agustingabrielli/projects/self-test-obsidian-plugin && npx jest --testPathPatterns sidebar 2>&1 | tail -30</automated>
  </verify>
  <done>npx jest --testPathPatterns sidebar passes (PASS). All 10 tests green. npx tsc --noEmit passes cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite stays green</name>
  <files>src/sidebar.ts</files>
  <action>
Run the full test suite. If any previously-passing tests regressed:
- Read the failing test output carefully
- Make targeted fixes to sidebar.ts only (do not modify generation.ts or its tests)
- Common issue: if sidebar.ts imports something not exported from the mock, the mock import path is `'obsidian'` which Jest resolves to `src/__mocks__/obsidian.ts`. Ensure all imported identifiers from 'obsidian' are exported from the mock.
- If TAbstractFile is referenced in sidebar.ts but not in the mock, add `export type TAbstractFile = TFile | TFolder;` to the mock.
  </action>
  <verify>
    <automated>cd /Users/agustingabrielli/projects/self-test-obsidian-plugin && npx jest 2>&1 | tail -20</automated>
  </verify>
  <done>Full jest suite passes. Both generation and sidebar test files are green. Zero failures.</done>
</task>

</tasks>

<verification>
`npx jest` passes - all test files green.
`npx tsc --noEmit` passes - no TypeScript errors.
`npx jest --testPathPatterns sidebar` shows all 10 tests passing with PASS label.
</verification>

<success_criteria>
- src/sidebar.ts exists with VIEW_TYPE_ACTIVE_RECALL, ActiveRecallSidebarView, getFolderStatuses, getLastGeneratedDate, buildActivateView, buildContextMenuHandler exported
- All 10 sidebar unit tests pass
- Full jest suite green (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-commands-and-sidebar/04-02-SUMMARY.md`
</output>
