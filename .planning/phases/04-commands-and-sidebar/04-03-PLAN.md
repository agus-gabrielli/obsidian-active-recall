---
phase: 04-commands-and-sidebar
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/main.ts
autonomous: false
requirements: [CMD-02, CMD-03]
user_setup: []
must_haves:
  truths:
    - "Right-clicking any folder shows 'Generate Self-Test' and triggers generation"
    - "Command palette has 'Open Active Recall Panel' that opens or focuses the sidebar"
    - "Ribbon icon opens/focuses the Active Recall panel"
    - "After generation from any entry point, sidebar reflects updated state without manual refresh"
    - "Disabling and re-enabling the plugin does not create duplicate sidebar panes"
  artifacts:
    - path: "src/main.ts"
      provides: "registerView, CMD-02, CMD-03, ribbon, onunload with detachLeavesOfType"
      contains: "registerView"
    - path: "src/sidebar.ts"
      provides: "ActiveRecallSidebarView (unchanged from Plan 02)"
      exports: ["VIEW_TYPE_ACTIVE_RECALL", "ActiveRecallSidebarView"]
  key_links:
    - from: "src/main.ts"
      to: "src/sidebar.ts"
      via: "import VIEW_TYPE_ACTIVE_RECALL, ActiveRecallSidebarView"
      pattern: "import.*ActiveRecallSidebarView.*sidebar"
    - from: "src/main.ts onunload"
      to: "workspace.detachLeavesOfType"
      via: "prevents duplicate panes"
      pattern: "detachLeavesOfType.*VIEW_TYPE_ACTIVE_RECALL"
    - from: "context menu onClick"
      to: "sidebarView.refresh()"
      via: "post-generate callback pattern"
      pattern: "refresh"
---

<objective>
Wire all four entry points into main.ts: register the sidebar ItemView, add CMD-02 ("Open Active Recall Panel"), add CMD-03 (folder context menu), add ribbon icon, clean up in onunload. End with human verification of the full integrated flow in Obsidian.

Purpose: Completes Phase 4 - all user-facing surfaces live. CMD-02 and CMD-03 requirements fulfilled.
Output: Updated src/main.ts; human-verified that all three entry points work and sidebar auto-refreshes
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-commands-and-sidebar/04-CONTEXT.md
@.planning/phases/04-commands-and-sidebar/04-RESEARCH.md
@.planning/phases/04-commands-and-sidebar/04-02-SUMMARY.md

<interfaces>
<!-- Current src/main.ts structure -->
import { Notice, Plugin } from 'obsidian';
import { ActiveRecallSettings, DEFAULT_SETTINGS, ActiveRecallSettingTab } from './settings';
import { GenerationService } from './generation';

export default class ActiveRecallPlugin extends Plugin {
    settings: ActiveRecallSettings;
    async onload() {
        // loads settings, adds settings tab
        // creates statusBarItem and generationService
        // registers CMD-01: 'generate-self-test'
    }
    onunload() {}
    async loadSettings() { ... }
    async saveSettings() { ... }
}

<!-- From src/sidebar.ts (Plan 02 output) -->
export const VIEW_TYPE_ACTIVE_RECALL = 'active-recall-panel';
export class ActiveRecallSidebarView extends ItemView {
    constructor(leaf: WorkspaceLeaf, _app: App, generationService: GenerationService)
    refresh(): void  // public - call after generation to re-render
    // getViewType, getDisplayText, getIcon, onOpen, onClose, renderPanel
}
export function buildActivateView(app: App, viewType: string): () => Promise<void>
export function buildContextMenuHandler(generationService: GenerationService, app: App, viewType: string): (menu: Menu, file: TAbstractFile) => void

<!-- Obsidian API patterns to use -->
// Register view (in onload, before any activation):
this.registerView(VIEW_TYPE_ACTIVE_RECALL, (leaf) => new ActiveRecallSidebarView(leaf, this.app, generationService));

// CMD-02:
this.addCommand({ id: 'open-active-recall-panel', name: 'Open Active Recall Panel', callback: () => activateView() });

// CMD-03 (context menu):
this.registerEvent(this.app.workspace.on('file-menu', buildContextMenuHandler(generationService, this.app, VIEW_TYPE_ACTIVE_RECALL)));

// Ribbon icon:
this.addRibbonIcon('brain-circuit', 'Open Active Recall Panel', () => activateView());

// onunload (CRITICAL - prevents duplicate panes):
onunload() {
    this.app.workspace.detachLeavesOfType(VIEW_TYPE_ACTIVE_RECALL);
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire sidebar view, CMD-02, CMD-03, and ribbon into main.ts</name>
  <files>src/main.ts</files>
  <action>
Update src/main.ts. Read the current file first, then make these additions:

1. Add imports at the top:
   ```typescript
   import { TFolder, TAbstractFile } from 'obsidian';
   import { VIEW_TYPE_ACTIVE_RECALL, ActiveRecallSidebarView, buildActivateView, buildContextMenuHandler } from './sidebar';
   ```
   Keep existing imports (`Notice`, `Plugin` from `obsidian`).

2. In `onload()`, after `generationService` is created and before the existing `addCommand` for CMD-01:

   a. Register the view:
   ```typescript
   this.registerView(
       VIEW_TYPE_ACTIVE_RECALL,
       (leaf) => new ActiveRecallSidebarView(leaf, this.app, generationService)
   );
   ```

   b. Build the activateView helper (scoped to onload):
   ```typescript
   const activateView = buildActivateView(this.app, VIEW_TYPE_ACTIVE_RECALL);
   ```

   c. Add CMD-02:
   ```typescript
   this.addCommand({
       id: 'open-active-recall-panel',
       name: 'Open Active Recall Panel',
       callback: () => activateView(),
   });
   ```

   d. Register context menu (CMD-03):
   ```typescript
   this.registerEvent(
       this.app.workspace.on(
           'file-menu',
           buildContextMenuHandler(generationService, this.app, VIEW_TYPE_ACTIVE_RECALL)
       )
   );
   ```

   e. Add ribbon icon:
   ```typescript
   this.addRibbonIcon('brain-circuit', 'Open Active Recall Panel', () => {
       activateView();
   });
   ```

3. Replace the empty `onunload()` with:
   ```typescript
   onunload() {
       this.app.workspace.detachLeavesOfType(VIEW_TYPE_ACTIVE_RECALL);
   }
   ```

4. Keep CMD-01 (`generate-self-test`) exactly as-is.

Ordering in onload matters: `registerView` must come before any `activateView` call, and all `addCommand`/`registerEvent`/`addRibbonIcon` calls go after `registerView`.

Do NOT remove or modify the existing CMD-01 `addCommand` block. Do NOT modify loadSettings or saveSettings.
  </action>
  <verify>
    <automated>cd /Users/agustingabrielli/projects/self-test-obsidian-plugin && npx tsc --noEmit 2>&1 && npx jest 2>&1 | tail -15</automated>
  </verify>
  <done>TypeScript compiles clean (zero errors). Full jest suite passes. src/main.ts includes registerView, CMD-02, CMD-03 context menu, ribbon icon, and detachLeavesOfType in onunload.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify all Phase 4 entry points in Obsidian</name>
  <action>Build and reload the plugin, then verify all 6 behavioral checks listed below in live Obsidian.</action>
  <what-built>
    Phase 4 complete - all three entry points wired:
    - CMD-02: "Open Active Recall Panel" in command palette
    - CMD-03: "Generate Self-Test" on folder right-click in file explorer
    - Ribbon: brain-circuit icon in left ribbon
    - Sidebar panel: two-section layout (Self-tests / No self-test)
    - Auto-refresh: sidebar updates after generation from any entry point
    - No duplicate panes: disable/re-enable plugin creates only one panel
  </what-built>
  <how-to-verify>
    Build and reload: run `npm run build` in the plugin directory, then in Obsidian: Settings > Community Plugins > reload the plugin (disable then enable, or use hot-reload if installed).

    **Check 1 - Ribbon icon:**
    - Verify brain-circuit ribbon icon appears in the left ribbon
    - Click it - the "Active Recall" panel should open in the right sidebar

    **Check 2 - CMD-02 (command palette):**
    - Press Cmd+P (or Ctrl+P on Windows)
    - Type "Open Active Recall Panel"
    - Command should appear; selecting it should open or focus the panel

    **Check 3 - Sidebar panel layout:**
    - With the panel open, verify it shows folders
    - Folders with an existing _self-test.md appear under "Self-tests" with a date and "Regenerate" button
    - Folders without appear under "No self-test" with a "Generate" button
    - Full folder paths shown (e.g., "StudyNotes/Week1" not just "Week1")
    - Empty sections are hidden (if no self-tests exist yet, no "Self-tests" header appears)

    **Check 4 - CMD-03 (context menu):**
    - Right-click any folder in the file explorer (left sidebar)
    - Verify "Generate Self-Test" appears in the context menu
    - Right-click a file (not a folder) - "Generate Self-Test" should NOT appear
    - Select "Generate Self-Test" on a folder - generation should run (status bar shows progress, Notice appears on completion)

    **Check 5 - Auto-refresh:**
    - With the Active Recall panel open, right-click a folder and select "Generate Self-Test"
    - After generation completes (Notice fires), the sidebar should move that folder from "No self-test" to "Self-tests" without closing/reopening the panel

    **Check 6 - No duplicate panes:**
    - Disable the plugin in Settings > Community Plugins
    - Re-enable it
    - Open the Active Recall panel
    - Verify only ONE "Active Recall" tab appears in the right sidebar (not two)
  </how-to-verify>
  <verify>Manual - see how-to-verify above</verify>
  <done>All 6 checks pass in live Obsidian. User types "approved".</done>
  <resume-signal>Type "approved" if all 6 checks pass, or describe which checks failed and what you observed</resume-signal>
</task>

</tasks>

<verification>
`npx tsc --noEmit` - zero errors.
`npx jest` - full suite green.
Human-verified all 6 behavioral checks in live Obsidian.
</verification>

<success_criteria>
- src/main.ts wires registerView, CMD-02, CMD-03, ribbon, and onunload detach
- All three entry points (command palette, context menu, ribbon) trigger generation or open the panel
- Sidebar auto-refreshes after generation from any entry point
- Disable/re-enable creates no duplicate panes
- All jest tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/04-commands-and-sidebar/04-03-SUMMARY.md`
</output>
