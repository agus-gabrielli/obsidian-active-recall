---
phase: 08-multi-provider-llm-dispatch
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/generation.ts
autonomous: false
requirements:
  - PROV-04
  - PROV-05
  - PROV-06

must_haves:
  truths:
    - "Selecting Gemini in settings and generating produces a valid _self-test.md"
    - "Selecting Claude in settings and generating produces a valid _self-test.md"
    - "Error Notices include the provider name when an API call fails"
  artifacts:
    - path: "src/generation.ts"
      provides: "Production-ready multi-provider dispatch"
      contains: "callGemini"
  key_links:
    - from: "Settings UI (provider dropdown)"
      to: "callLLM dispatcher"
      via: "settings.provider passed through GenerationService"
      pattern: "this\\.settings\\.provider"
---

<objective>
Build the plugin and human-verify that Gemini and Claude API calls work end-to-end in Obsidian with real API keys, and that error messages show the correct provider name.

Purpose: Unit tests validate request shapes and response parsing, but only a live test confirms the full integration works - real API responses, real Notice rendering, real settings wiring.
Output: Verified working plugin with all three providers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-multi-provider-llm-dispatch/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build and full test suite</name>
  <files>src/generation.ts</files>
  <read_first>
    - src/generation.ts
  </read_first>
  <action>
Run the full test suite and production build to confirm everything compiles and passes:

1. Run `npx jest` - all tests must pass with 0 failures.
2. Run `npx tsc --noEmit` - no TypeScript errors.
3. Run `npm run build` - production build succeeds, `main.js` is emitted.
4. Copy `main.js` and `styles.css` to the test vault if the esbuild onEnd plugin does not already handle it (check the build output).

If any step fails, fix the issue in `src/generation.ts` before proceeding. Do NOT introduce new features - only fix build/test issues from Plan 01.
  </action>
  <verify>
    <automated>npx jest 2>&1 | tail -3 && npx tsc --noEmit 2>&1 | tail -3 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest` output contains "Tests:" with 0 failures
    - `npx tsc --noEmit` produces no error output
    - `npm run build` exits cleanly and `main.js` exists in project root
  </acceptance_criteria>
  <done>Full test suite passes, TypeScript compiles cleanly, production build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify Gemini, Claude, and error handling in Obsidian</name>
  <files>main.js</files>
  <action>
Present the human-verify checklist to the user. The plugin has been built and is ready for live testing in Obsidian. The user needs to reload the plugin and test all three providers with real API keys, plus verify error messages include the provider name.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification"</automated>
  </verify>
  <what-built>
Multi-provider LLM dispatch: the plugin now calls Gemini (Google AI Studio) and Claude (Anthropic) APIs in addition to OpenAI, with provider-specific error messages.
  </what-built>
  <how-to-verify>
**Prerequisites:** You need API keys for at least two of the three providers. If you only have one non-OpenAI key, verify what you can and skip the rest.

**Gemini verification (PROV-04):**
1. Open Obsidian, go to plugin settings
2. Select "Gemini" from the Provider dropdown
3. Enter your Google AI Studio API key
4. Select a model (e.g. gemini-2.5-flash)
5. Navigate to a folder with 2-3 short notes
6. Right-click the folder and select "Generate Self-Test"
7. Verify: a `_self-test.md` is created with concept map, categorized questions, hints, and reference answers
8. Verify: the content is coherent and formatted correctly (not garbled or empty)

**Claude verification (PROV-05):**
9. Go back to settings, select "Claude (Anthropic)" from the Provider dropdown
10. Enter your Anthropic API key
11. Select a model (e.g. claude-sonnet-4-6)
12. Generate a self-test for the same or different folder
13. Verify: a `_self-test.md` is created with proper formatting
14. Verify: content quality is reasonable

**Error message verification (PROV-06):**
15. With Gemini selected, enter an invalid API key (e.g. "bad-key")
16. Try to generate - verify the Notice says something containing "Gemini" (e.g. "Gemini API key invalid")
17. With Claude selected, enter an invalid API key
18. Try to generate - verify the Notice says something containing "Claude (Anthropic)"

**OpenAI regression:**
19. Switch back to OpenAI, enter your OpenAI key
20. Generate a self-test - verify it still works as before
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which checks failed and what you observed.</resume-signal>
  <done>Human has verified all three providers work in Obsidian and error messages include provider names.</done>
</task>

</tasks>

<verification>
- All three providers produce valid _self-test.md output in Obsidian
- Error messages include provider name
- No regression in OpenAI generation
- Full test suite passes
- Production build clean
</verification>

<success_criteria>
1. Human confirms Gemini generation produces a valid self-test (PROV-04)
2. Human confirms Claude generation produces a valid self-test (PROV-05)
3. Human confirms error Notices include the provider name (PROV-06)
4. Human confirms OpenAI still works (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-provider-llm-dispatch/08-02-SUMMARY.md`
</output>
