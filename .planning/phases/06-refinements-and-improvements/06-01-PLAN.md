---
phase: 06-refinements-and-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/prompts.ts
  - src/generation.ts
  - src/__tests__/generation.test.ts
autonomous: true
requirements:
  - REFINE-PROMPTS
must_haves:
  truths:
    - "All LLM prompt text lives in src/prompts.ts, not generation.ts"
    - "Concept map output is a Mermaid mindmap diagram, not a flat bullet list"
    - "Questions within each category are ordered general/simple to complex/specific"
    - "Hints are contextual cues that situate the concept without revealing the answer"
    - "Check answers include source traceability with Obsidian wiki-links"
    - "Check answers provide explanations that help understanding, not just validation"
    - "Existing tests still pass after refactor"
  artifacts:
    - path: "src/prompts.ts"
      provides: "All LLM prompt templates and render function"
      exports: ["SYSTEM_MESSAGE", "batchTemplate", "synthesisTemplate", "render"]
    - path: "src/generation.ts"
      provides: "Batching, API calls, error handling - no prompt text"
      contains: "import.*from.*prompts"
  key_links:
    - from: "src/generation.ts"
      to: "src/prompts.ts"
      via: "import and render() calls"
      pattern: "import.*from.*prompts"
    - from: "src/prompts.ts"
      to: "src/generation.ts"
      via: "render(template, vars) called in buildBatchPrompt/buildSynthesisPrompt"
      pattern: "render\\("
---

<objective>
Extract all LLM prompts from generation.ts into a dedicated src/prompts.ts module, then improve prompt quality: Mermaid mindmap concept maps, explicit question ordering, contextual hint cues, detailed check answers with source traceability via Obsidian wiki-links.

Purpose: Separate prompt content from orchestration logic, and substantially improve the quality of generated self-tests.
Output: New src/prompts.ts with all prompt templates; refactored generation.ts that imports and renders them; all existing tests passing.
</objective>

<execution_context>
@/Users/agustingabrielli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agustingabrielli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-refinements-and-improvements/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/prompts.ts with templates and render function</name>
  <files>src/prompts.ts, src/generation.ts</files>
  <read_first>src/generation.ts, src/settings.ts, .planning/phases/06-refinements-and-improvements/06-CONTEXT.md</read_first>
  <action>
Create new file `src/prompts.ts` containing:

1. A `render(template: string, vars: Record<string, string>): string` function that replaces `{{key}}` placeholders with values from the vars object. Use simple string replace (loop over keys, replace all occurrences of `{{key}}`).

2. Export `SYSTEM_MESSAGE = 'You are an expert educator creating active recall study materials.'`

3. Export `batchTemplate` - a template string that replaces the current `buildBatchPrompt` prompt text. The template must include these `{{placeholders}}`:
   - `{{noteBlocks}}` - the concatenated note content
   - `{{conceptMapInstruction}}` - conditional concept map section
   - `{{hintInstruction}}` - conditional hint instructions
   - `{{checkInstruction}}` - conditional check answer instructions
   - `{{languageInstruction}}` - conditional language override
   - `{{customInstruction}}` - conditional custom instructions

   **Concept map instruction (CHANGED from flat bullets to Mermaid mindmap):**
   When concept map is enabled, the instruction must be:
   ```
   ## Concept Map

   Create a Mermaid mindmap diagram showing the key concepts and their relationships.
   Use this exact syntax:

   ```mermaid
   mindmap
     root((Topic))
       Category A
         Concept 1
         Concept 2
       Category B
         Concept 3
   ```

   Rules:
   - Root node: use the folder topic as the central concept
   - Limit to 3-5 top-level categories maximum
   - Each category should have 2-4 subconcepts maximum
   - Order categories from general/foundational to specific/advanced
   - Include only key concepts and important relationships - no exhaustive lists
   - Use short labels (1-3 words per node)

   (Then add a horizontal rule: ---)
   ```

   **Question ordering instruction (ENHANCED):**
   After "Generate questions organized into categories." add: "Within each category, order questions from general and simple to complex and specific. Start with questions that test basic understanding, then progress to questions requiring deeper analysis or synthesis."

   **Hint instruction (ENHANCED - when hints enabled):**
   ```
   - After each question, add a collapsible hint using this exact callout syntax:
     > [!hint]-
     > Your hint text here
     (blank line required after the callout block)
   - Hints must be contextual cues that situate the concept without revealing the answer.
   - Good hint example: "Think about the training loop - what happens after the forward pass computes the loss?"
   - Hints should trigger recall through associations, context, or indirect cues.
   - If a useful hint cannot be generated without being too obvious, omit the hint entirely for that question.
   ```

   **Check answer instruction (ENHANCED - when check answers enabled):**
   ```
   - After each hint (or question if hints are disabled), add a collapsible reference answer with a blank line before it:

     > [!check]-
     > Your reference answer here
   - Reference answers should provide explanations that help understanding, not just validate the answer. Add context or clarifications when relevant to reinforce learning.
   - At the end of each reference answer, add a source line using Obsidian wiki-link syntax referencing which note(s) the answer is based on. Format: Source: [[Note A]], [[Note B]]
   - The note names come from the === Note: name === headers in the content above.
   ```

4. Export `synthesisTemplate` - same structure but for the synthesis prompt. Include the same enhanced instructions for concept map (Mermaid mindmap), hint quality, check answer quality, and source traceability.

5. Export helper functions that build the conditional instruction strings from settings:
   - `buildConceptMapInstruction(enabled: boolean): string`
   - `buildHintInstruction(enabled: boolean): string`
   - `buildCheckInstruction(enabled: boolean): string`
   - `buildLanguageInstruction(language: string): string`
   - `buildCustomInstruction(instructions: string): string`

Then modify `src/generation.ts`:
- Import `SYSTEM_MESSAGE`, `batchTemplate`, `synthesisTemplate`, `render`, and the helper functions from `./prompts`
- Remove the `SYSTEM_MESSAGE` const from `GenerationService.generate()` (line 247)
- Replace inline prompt construction in `buildBatchPrompt` with: build vars object using helper functions, then `return render(batchTemplate, vars)`
- Replace inline prompt construction in `buildSynthesisPrompt` with: build vars object using helper functions, then `return render(synthesisTemplate, vars)`
- Delete `buildFormattingInstructions` function entirely (replaced by individual helpers in prompts.ts)
- Keep `buildBatchPrompt` and `buildSynthesisPrompt` as exported functions in generation.ts (they remain the public API), but their bodies now delegate to render()
  </action>
  <verify>
    <automated>cd /Users/agustingabrielli/projects/self-test-obsidian-plugin && npx jest --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/prompts.ts exists and exports: SYSTEM_MESSAGE, batchTemplate, synthesisTemplate, render, buildConceptMapInstruction, buildHintInstruction, buildCheckInstruction, buildLanguageInstruction, buildCustomInstruction
    - src/generation.ts contains `import` from `'./prompts'`
    - src/generation.ts does NOT contain the string `You are creating an active recall self-test` (old inline prompt moved to prompts.ts)
    - src/generation.ts does NOT contain `buildFormattingInstructions` function
    - src/prompts.ts batchTemplate contains `mindmap` (Mermaid concept map)
    - src/prompts.ts batchTemplate contains `general and simple to complex and specific` (question ordering)
    - src/prompts.ts contains `contextual cues that situate the concept without revealing` (hint quality)
    - src/prompts.ts contains `Source: [[Note A]], [[Note B]]` (source traceability example)
    - src/prompts.ts contains `explanations that help understanding` (check answer quality)
    - All existing tests pass: `npx jest --no-coverage` exits 0
  </acceptance_criteria>
  <done>All LLM prompt text lives in src/prompts.ts with improved instructions for Mermaid concept maps, question ordering, contextual hints, detailed check answers with wiki-link source traceability. generation.ts delegates to render(). All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests for prompts.ts render function and template outputs</name>
  <files>src/__tests__/prompts.test.ts</files>
  <read_first>src/prompts.ts, src/__tests__/generation.test.ts</read_first>
  <behavior>
    - Test: render() replaces {{key}} placeholders with provided values
    - Test: render() leaves unmatched placeholders as empty strings or removes them
    - Test: buildConceptMapInstruction(true) contains "mindmap" and "mermaid"
    - Test: buildConceptMapInstruction(false) returns empty string
    - Test: buildHintInstruction(true) contains "contextual cues"
    - Test: buildHintInstruction(false) returns empty string
    - Test: buildCheckInstruction(true) contains "Source: [[Note A]]"
    - Test: buildCheckInstruction(false) returns empty string
    - Test: batchTemplate contains "general and simple to complex and specific"
    - Test: Full render of batchTemplate with all options enabled produces output containing mindmap, hint callout syntax, check callout syntax, and source traceability
  </behavior>
  <action>
Create `src/__tests__/prompts.test.ts` following TDD (RED then GREEN - but since Task 1 already created prompts.ts, write the tests to validate the implementation):

- Import render, batchTemplate, synthesisTemplate, SYSTEM_MESSAGE, and all builder functions from '../prompts'
- Import ActiveRecallSettings type from '../settings'
- Test render() with simple substitution: `render('Hello {{name}}', { name: 'World' })` === 'Hello World'
- Test render() with multiple placeholders
- Test each builder function for enabled=true and enabled=false
- Test that batchTemplate rendered with all settings enabled contains key strings: "mindmap", "[!hint]-", "[!check]-", "Source:", "general and simple to complex"
- Test that synthesisTemplate rendered with all settings enabled contains the same key strings
- Follow the same Jest patterns as generation.test.ts (beforeEach, describe blocks)
  </action>
  <verify>
    <automated>cd /Users/agustingabrielli/projects/self-test-obsidian-plugin && npx jest src/__tests__/prompts.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/prompts.test.ts exists
    - File contains at least 8 test cases (describe/test blocks)
    - File imports from '../prompts'
    - `npx jest src/__tests__/prompts.test.ts --no-coverage` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Prompt module has dedicated test coverage validating render(), all builder functions, and template content for Mermaid, hints, check answers, and source traceability.</done>
</task>

</tasks>

<verification>
- `npx jest --no-coverage` - all tests pass (generation + sidebar + prompts)
- `npx tsc --noEmit` - no type errors
- `src/prompts.ts` exists with all exported templates and helpers
- `src/generation.ts` imports from `./prompts` and contains no inline prompt text
</verification>

<success_criteria>
- All LLM prompts extracted to src/prompts.ts
- Mermaid mindmap replaces flat bullet concept map
- Question ordering explicitly instructs general-to-complex within categories
- Hint instructions require contextual cues, allow omission when too obvious
- Check answers require explanations and Source: [[Note]] wiki-links
- render() function handles template substitution
- All tests (old + new) pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-refinements-and-improvements/06-01-SUMMARY.md`
</output>
