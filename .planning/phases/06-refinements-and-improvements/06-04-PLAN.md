---
phase: 06-refinements-and-improvements
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02", "06-03"]
files_modified: []
autonomous: false
requirements:
  - REFINE-VERIFY
must_haves:
  truths:
    - "All refinements verified working in Obsidian"
    - "Plugin builds without errors"
    - "All tests pass"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 6 refinements work correctly: build the plugin, run all tests, and have the user verify the changes in Obsidian.

Purpose: Ensure all refinements (prompt improvements, model dropdown, sidebar spinner, README) are working before Phase 7 final release.
Output: Human-verified confirmation that all changes work correctly.
</objective>

<execution_context>
@/Users/agustingabrielli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agustingabrielli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-refinements-and-improvements/06-CONTEXT.md
@.planning/phases/06-refinements-and-improvements/06-01-SUMMARY.md
@.planning/phases/06-refinements-and-improvements/06-02-SUMMARY.md
@.planning/phases/06-refinements-and-improvements/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run all tests</name>
  <files></files>
  <read_first>package.json</read_first>
  <action>
Run the full test suite and production build to confirm everything compiles and passes:

1. `npx jest --no-coverage` - all tests must pass (generation, sidebar, prompts)
2. `npm run build` - production build must succeed without errors
3. Verify the build output exists: `main.js` in project root

Report the test count and build status.
  </action>
  <verify>
    <automated>cd /Users/agustingabrielli/projects/self-test-obsidian-plugin && npx jest --no-coverage 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --no-coverage` exits 0 with all tests passing
    - `npm run build` exits 0
    - `main.js` exists in project root after build
  </acceptance_criteria>
  <done>All tests pass and production build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 6 refinements in Obsidian</name>
  <files></files>
  <action>
Present the verification checklist to the user. All four areas of refinement must be verified in the running Obsidian plugin:

Phase 6 refinements built:
1. Prompt templates extracted to src/prompts.ts with improved instructions (Mermaid concept map, ordered questions, contextual hints, detailed check answers with source wiki-links)
2. Model selection dropdown in settings (5 curated models + custom option)
3. Sidebar loading spinner during generation (prevents double-generation)
4. README science section (3 paper references) and differentiation from LLM Test Generator

Verification steps:

**Settings verification:**
1. Open Obsidian, go to Settings > AI Active Recall
2. Verify Model field is now a dropdown showing: gpt-4o, gpt-4o-mini, gpt-4.1, gpt-4.1-mini, gpt-4.1-nano
3. Select "Custom model..." from the dropdown - verify a text input appears below
4. Select "gpt-4o-mini" from the dropdown - verify the text input disappears

**Sidebar loading verification:**
5. Open the Active Recall sidebar panel
6. Click Generate or Regenerate on any folder
7. Verify the button is replaced with a spinner and "Generating..." text
8. Verify the button reappears after generation completes

**Generation quality verification:**
9. Open the generated _self-test.md file
10. Verify concept map uses Mermaid mindmap syntax (```mermaid / mindmap / root((...)))
11. Verify questions within each category go from simpler to more complex
12. Verify hints are contextual cues (not just restating the question)
13. Verify check answers include Source: [[Note Name]] wiki-links
14. Verify check answers provide explanations, not just one-line validations

**README verification:**
15. Open README.md
16. Verify "Why active recall works" section exists with paper references
17. Verify "How is this different from LLM Test Generator?" section exists
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User has verified all 17 checks and approved the Phase 6 refinements.</done>
</task>

</tasks>

<verification>
- Build succeeds
- All tests pass
- Human verifies all 4 areas of refinement in Obsidian
</verification>

<success_criteria>
- Plugin builds and tests pass
- User confirms model dropdown works correctly
- User confirms sidebar spinner appears during generation
- User confirms generated output quality improvements (Mermaid, hints, sources)
- User confirms README additions
</success_criteria>

<output>
After completion, create `.planning/phases/06-refinements-and-improvements/06-04-SUMMARY.md`
</output>
