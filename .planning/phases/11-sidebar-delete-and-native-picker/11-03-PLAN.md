---
phase: 11-sidebar-delete-and-native-picker
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified: []
autonomous: false
requirements:
  - DEL-01
  - DEL-02
  - PICK-01

must_haves:
  truths:
    - "Production build succeeds with no errors"
    - "All tests pass"
    - "Trash icon visible on sidebar rows, hidden during generation"
    - "Delete confirmation shows file path and removes file on confirm"
    - "Linked notes picker uses native fuzzy search modal"
    - "Zero-links note loops back to picker"
    - "Generated self-test frontmatter only contains source_mode, source, source_notes (no spaced repetition fields)"
  artifacts: []
  key_links: []
---

<objective>
Run production build, verify all tests pass, and human-verify both features (trash icon delete and native linked notes picker) in Obsidian.

Purpose: Confirm both features work correctly in the live plugin before closing the phase.
Output: Verified working features, production build artifact.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-sidebar-delete-and-native-picker/11-CONTEXT.md
@.planning/phases/11-sidebar-delete-and-native-picker/11-01-SUMMARY.md
@.planning/phases/11-sidebar-delete-and-native-picker/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build and test verification</name>
  <read_first>
    - src/sidebar.ts (verify trash icon and openLinkedNotesPicker integration)
    - src/modals.ts (verify NotePickerModal, LinkConfirmModal, DeleteConfirmModal present)
    - src/main.ts (verify import updates)
  </read_first>
  <files></files>
  <action>
Run the following checks in sequence:

1. `npx tsc --noEmit` - must exit 0 with no type errors
2. `npx jest --no-cache` - must exit 0 with all tests passing
3. `node esbuild.config.mjs` (or the project's build command) - production bundle must build without errors
4. Report test count and bundle size
  </action>
  <verify>
    <automated>cd /Users/agustingabrielli/projects/self-test-obsidian-plugin && npx tsc --noEmit && npx jest --no-cache && node esbuild.config.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npx jest --no-cache` exits 0 and reports all tests passing
    - Production build creates main.js without errors
    - No `LinkedNotesPickerModal` references remain in src/sidebar.ts or src/main.ts
  </acceptance_criteria>
  <done>
    TypeScript compiles clean, all tests pass, production bundle builds successfully.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify delete and native picker in Obsidian</name>
  <what-built>
    Two sidebar features:
    1. Trash icon on every self-test row (all three tabs) with confirmation modal
    2. Linked notes picker replaced with native FuzzySuggestModal two-step flow
  </what-built>
  <how-to-verify>
    **Trash icon delete (all tabs):**
    1. Open the Active Recall sidebar
    2. On the Folders tab, verify each generated folder row shows a trash icon to the right of Regenerate
    3. Click the trash icon on any row - a confirmation modal should appear showing the file path
    4. Click Cancel - nothing should happen, file remains
    5. Click the trash icon again, then click Delete - the file should be deleted and the row disappears immediately
    6. Switch to Tags tab - verify trash icon appears on tag-based self-test rows
    7. Switch to Links tab - verify trash icon appears on link-based self-test rows
    8. Trigger a generation (e.g., regenerate a folder) - verify the trash icon disappears while the spinner is showing
    9. After generation completes, verify the trash icon reappears

    **Frontmatter cleanup:**
    10. Open any recently generated self-test file and check its YAML frontmatter - it should only have `source_mode`, `source`, and `source_notes` (no `last_review`, `next_review`, `review_count`, `review_interval_days`)

    **Linked notes picker (two-step flow):**
    11. Open command palette and run "Generate Self-Test from Linked Notes"
    12. A fuzzy search modal should appear (native Obsidian feel) - type a few characters and verify fuzzy matching works
    13. Verify each suggestion shows the note basename prominently with a dimmed path underneath
    14. Select a note that HAS links - a confirmation modal should appear with:
        - The root note name
        - A depth-2 toggle checkbox
        - A preview count (e.g., "5 notes will be collected")
        - A Generate button
    15. Toggle the depth-2 checkbox - the preview count should update
    16. Click Generate - generation should start and the sidebar should show the Links tab with progress
    17. (If possible) Select a note with ZERO links - you should see a Notice saying "no linked notes" and the fuzzy picker should reopen
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Production build succeeds
- All automated tests pass
- Human confirms both features work as specified in the 16-point checklist
</verification>

<success_criteria>
Both features verified working in Obsidian: trash icon deletes self-tests with confirmation across all tabs, and linked notes picker uses native FuzzySuggestModal with two-step flow including zero-links guard.
</success_criteria>

<output>
After completion, create `.planning/phases/11-sidebar-delete-and-native-picker/11-03-SUMMARY.md`
</output>
