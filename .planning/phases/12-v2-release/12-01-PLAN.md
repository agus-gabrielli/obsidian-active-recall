---
phase: 12-v2-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - package.json
  - package-lock.json
  - versions.json
  - LICENSE
  - styles.css
  - src/main.ts
  - src/sidebar.ts
  - src/settings.ts
  - src/generation.ts
  - src/modals.ts
  - src/__tests__/sidebar.test.ts
  - src/__tests__/generation.test.ts
  - src/__mocks__/obsidian.ts
autonomous: true
requirements:
  - DIST-04

must_haves:
  truths:
    - "Plugin ID is 'self-test' everywhere"
    - "All TypeScript class names use SelfTest prefix instead of ActiveRecall"
    - "All CSS classes use self-test-* prefix instead of active-recall-*"
    - "All user-facing strings say 'Self Test' instead of 'Active Recall'"
    - "All 137+ tests pass after rename"
    - "TypeScript compilation succeeds with zero errors"
    - "LICENSE has correct copyright holder"
  artifacts:
    - path: "manifest.json"
      provides: "Plugin metadata with new ID and name"
      contains: "\"id\": \"self-test\""
    - path: "styles.css"
      provides: "All CSS class definitions"
      contains: ".self-test-panel"
    - path: "src/sidebar.ts"
      provides: "Sidebar view with renamed constants and class"
      exports: ["VIEW_TYPE_SELF_TEST", "SelfTestSidebarView"]
    - path: "src/main.ts"
      provides: "Plugin entry with renamed class"
      contains: "class SelfTestPlugin"
  key_links:
    - from: "src/main.ts"
      to: "src/sidebar.ts"
      via: "import VIEW_TYPE_SELF_TEST, SelfTestSidebarView"
      pattern: "VIEW_TYPE_SELF_TEST"
    - from: "src/main.ts"
      to: "src/settings.ts"
      via: "import SelfTestSettings, SelfTestSettingTab"
      pattern: "SelfTestSettings"
    - from: "styles.css"
      to: "src/sidebar.ts"
      via: "CSS class name strings match"
      pattern: "self-test-"
---

<objective>
Rename the plugin from "AI Active Recall" to "Self Test" across every source file, test file, JSON config, and CSS definition in a single atomic operation. Also update LICENSE copyright holder.

Purpose: Prepare the plugin identity for first public release (per D-01 through D-06). The rename must be atomic because TypeScript compilation rejects partial renames.
Output: All source files renamed, tests green, build clean.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-v2-release/12-CONTEXT.md
@.planning/phases/12-v2-release/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic rename across all files + LICENSE update</name>
  <files>manifest.json, package.json, versions.json, LICENSE, styles.css, src/main.ts, src/sidebar.ts, src/settings.ts, src/generation.ts, src/modals.ts, src/__tests__/sidebar.test.ts, src/__tests__/generation.test.ts, src/__mocks__/obsidian.ts</files>
  <read_first>manifest.json, package.json, versions.json, LICENSE, styles.css, src/main.ts, src/sidebar.ts, src/settings.ts, src/generation.ts, src/modals.ts, src/__tests__/sidebar.test.ts, src/__tests__/generation.test.ts, src/__mocks__/obsidian.ts</read_first>
  <action>
Execute the full rename in one pass. Every replacement is listed below - apply all before committing.

**manifest.json** (per D-01, D-14):
- `"id": "ai-active-recall"` -> `"id": "self-test"`
- `"name": "AI Active Recall"` -> `"name": "Self Test"`
- `"description": "Generate active recall self-tests from your notes using AI."` -> `"description": "Generate self-testing questions from your notes using AI to practice active recall."`
- Keep `"version": "1.0.0"`, `"isDesktopOnly": true`, all other fields unchanged

**package.json** (per D-05):
- `"name": "obsidian-sample-plugin"` -> `"name": "self-test"`
- `"description": "This is a sample plugin for Obsidian (https://obsidian.md)"` -> `"description": "Generate self-testing questions from your notes using AI to practice active recall."`

**versions.json**: No change needed - `"1.0.0": "0.15.0"` is already correct per D-13.

**LICENSE**: Replace entire first line:
- `Copyright (C) 2020-2025 by Dynalist Inc.` -> `Copyright (C) 2025-2026 by Agustin Gabrielli`
- Keep the rest of the 0-BSD license text unchanged.

**styles.css** (per D-03):
Replace every occurrence of `active-recall-` with `self-test-` in CSS selectors and animation references. Specifically:
- All 23 class selectors: `.active-recall-panel`, `.active-recall-header`, `.active-recall-title`, `.active-recall-description`, `.active-recall-section`, `.active-recall-section-label`, `.active-recall-folder-row`, `.active-recall-folder-info`, `.active-recall-folder-name`, `.active-recall-date`, `.active-recall-btn`, `.active-recall-loading`, `.active-recall-spinner`, `.active-recall-loading-text`, `.active-recall-tab-bar`, `.active-recall-tab`, `.active-recall-tab--active`, `.active-recall-row--clickable`, `.active-recall-generate-new-btn`, `.active-recall-empty-state`, `.active-recall-toast`, `.active-recall-toast--visible`, `.active-recall-toast--hiding`, `.active-recall-trash-btn`, `.active-recall-confirm-buttons`, `.active-recall-note-suggestion`, `.active-recall-note-name`, `.active-recall-note-path`
- Animation reference: `animation: active-recall-spin` -> `animation: self-test-spin`
- Keyframe: `@keyframes active-recall-spin` -> `@keyframes self-test-spin`

**src/sidebar.ts** (per D-02, D-03, D-04):
- `VIEW_TYPE_ACTIVE_RECALL = 'active-recall-panel'` -> `VIEW_TYPE_SELF_TEST = 'self-test-panel'`
- `class ActiveRecallSidebarView` -> `class SelfTestSidebarView`
- `import type ActiveRecallPlugin` -> update to `import type SelfTestPlugin` (match whatever the import actually says)
- `getDisplayText()` return value: `'Active Recall'` -> `'Self Test'`
- Header text string `'Active Recall'` -> `'Self Test'`
- All CSS class string literals: replace `'active-recall-` with `'self-test-` (~25 occurrences)
- Update all exports to use new names

**src/main.ts** (per D-02, D-04):
- `class ActiveRecallPlugin` -> `class SelfTestPlugin`
- All import references: `ActiveRecallSettings` -> `SelfTestSettings`, `ActiveRecallSettingTab` -> `SelfTestSettingTab`, `VIEW_TYPE_ACTIVE_RECALL` -> `VIEW_TYPE_SELF_TEST`, `ActiveRecallSidebarView` -> `SelfTestSidebarView`
- `openSidebarWithTab` parameter type: `ActiveRecallPlugin` -> `SelfTestPlugin`
- `getSidebarView` return type: `ActiveRecallSidebarView` -> `SelfTestSidebarView`
- Command id: `'open-active-recall-panel'` -> `'open-self-test-panel'`
- Command name: `'Open Active Recall Panel'` -> `'Open Self Test Panel'`
- Ribbon tooltip: `'Open Active Recall Panel'` -> `'Open Self Test Panel'`
- `export default` class reference if present

**src/settings.ts** (per D-02):
- `interface ActiveRecallSettings` -> `interface SelfTestSettings`
- `class ActiveRecallSettingTab` -> `class SelfTestSettingTab`
- `import type ActiveRecallPlugin` -> `import type SelfTestPlugin`
- `DEFAULT_SETTINGS` type annotation: `ActiveRecallSettings` -> `SelfTestSettings`
- Constructor parameter type: `ActiveRecallPlugin` -> `SelfTestPlugin`
- All other type annotations using these names

**src/generation.ts** (per D-02):
- `import { ActiveRecallSettings }` or `import type { ActiveRecallSettings }` -> `SelfTestSettings`
- All local type annotations: `ActiveRecallSettings` -> `SelfTestSettings`

**src/modals.ts** (per D-03):
- `'active-recall-tag-suggestion'` -> `'self-test-tag-suggestion'`
- `'active-recall-tag-count'` -> `'self-test-tag-count'`
- `'active-recall-note-suggestion'` -> `'self-test-note-suggestion'`
- `'active-recall-note-name'` -> `'self-test-note-name'`
- `'active-recall-note-path'` -> `'self-test-note-path'`
- `'active-recall-depth-toggle'` -> `'self-test-depth-toggle'`
- `'active-recall-link-preview'` -> `'self-test-link-preview'`
- `'mod-cta active-recall-generate-btn'` -> `'mod-cta self-test-generate-btn'`
- `'active-recall-confirm-buttons'` -> `'self-test-confirm-buttons'`

**src/__tests__/sidebar.test.ts** (per D-03):
- `'active-recall-panel'` -> `'self-test-panel'`
- `'active-recall-tab-bar'` -> `'self-test-tab-bar'`
- `'active-recall-loading'` -> `'self-test-loading'` (3 occurrences)
- `'active-recall-trash-btn'` -> `'self-test-trash-btn'` (3 occurrences)
- `import { ActiveRecallSidebarView` -> `import { SelfTestSidebarView`
- All test references to the class name

**src/__tests__/generation.test.ts**:
- `import type { ActiveRecallSettings }` -> `import type { SelfTestSettings }`
- All local variable type annotations: `ActiveRecallSettings` -> `SelfTestSettings` (3+ occurrences)

**src/__mocks__/obsidian.ts** (comments only):
- Update comment mentioning `ActiveRecallSettingTab` -> `SelfTestSettingTab`
- Update comment mentioning `ActiveRecallSidebarView` -> `SelfTestSidebarView`

After all file edits, run `npm install` to sync package-lock.json with the updated package.json name.
  </action>
  <verify>
    <automated>npx jest --bail && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "active-recall" manifest.json package.json styles.css src/ --include="*.ts" --include="*.css" | grep -v node_modules | grep -v ".planning"` returns zero results (no old name remnants in source)
    - `grep -r "ActiveRecall" src/ --include="*.ts" | grep -v node_modules` returns zero results
    - `grep '"id": "self-test"' manifest.json` finds a match
    - `grep '"name": "Self Test"' manifest.json` finds a match
    - `grep '"name": "self-test"' package.json` finds a match
    - `grep "Agustin Gabrielli" LICENSE` finds a match
    - `grep "Dynalist" LICENSE` returns zero results
    - `npx jest` exits 0 with 137+ tests passing
    - `npm run build` exits 0 (tsc -noEmit succeeds + esbuild produces main.js)
  </acceptance_criteria>
  <done>Every occurrence of "active-recall", "ActiveRecall", and "AI Active Recall" is replaced with the "self-test"/"SelfTest"/"Self Test" equivalents across all source, test, config, CSS, and license files. Tests pass. Build succeeds.</done>
</task>

</tasks>

<verification>
- `grep -r "active-recall\|ActiveRecall\|Active Recall" manifest.json package.json styles.css src/ --include="*.ts" --include="*.css" --include="*.json" | grep -v node_modules | grep -v ".planning"` returns zero lines
- `npx jest` - all 137+ tests pass
- `npm run build` - zero TS errors, main.js produced
</verification>

<success_criteria>
- Zero occurrences of old naming in any source/config/CSS file
- All tests pass
- TypeScript compilation clean
- LICENSE copyright updated to Agustin Gabrielli
</success_criteria>

<output>
After completion, create `.planning/phases/12-v2-release/12-01-SUMMARY.md`
</output>
