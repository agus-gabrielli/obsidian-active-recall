---
phase: 12-v2-release
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
files_modified: []
autonomous: false
requirements:
  - DIST-04

must_haves:
  truths:
    - "Production build succeeds with renamed plugin"
    - "GitHub release 1.0.0 has updated assets (main.js, styles.css, manifest.json)"
    - "Plugin loads in Obsidian with 'Self Test' name"
    - "PR is open against obsidianmd/obsidian-releases"
  artifacts:
    - path: "main.js"
      provides: "Production build output"
    - path: "manifest.json"
      provides: "Plugin metadata"
      contains: "\"id\": \"self-test\""
  key_links:
    - from: "manifest.json"
      to: "GitHub release"
      via: "version tag match"
      pattern: "1.0.0"
---

<objective>
Build production bundle, update the existing GitHub release 1.0.0 with new assets, human-verify the renamed plugin loads correctly in Obsidian, and submit the plugin to the Obsidian community store.

Purpose: Complete DIST-04 - get the plugin published in the Obsidian community store.
Output: Updated GitHub release + open PR against obsidianmd/obsidian-releases.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-v2-release/12-CONTEXT.md
@.planning/phases/12-v2-release/12-RESEARCH.md
@.planning/phases/12-v2-release/12-01-SUMMARY.md
@.planning/phases/12-v2-release/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build and GitHub release update</name>
  <files></files>
  <read_first>manifest.json, package.json, versions.json</read_first>
  <action>
1. Run `npx jest` to confirm all tests pass.
2. Run `npm run build` to produce a clean production build with the renamed identifiers.
3. Verify the built main.js contains the new names:
   - `grep "self-test" main.js | head -3` should show matches
   - `grep "active-recall" main.js` should return zero results
4. Update the existing GitHub release tagged `1.0.0`:
   - Delete the three old assets from the release: `gh release delete-asset 1.0.0 main.js`, `gh release delete-asset 1.0.0 manifest.json`, `gh release delete-asset 1.0.0 styles.css`
   - Upload the three new assets: `gh release upload 1.0.0 main.js manifest.json styles.css`
   - Update the release title and notes if needed: `gh release edit 1.0.0 --title "1.0.0"`
5. Verify the release: `gh release view 1.0.0` should show 3 assets attached.
  </action>
  <verify>
    <automated>npm run build && grep -c "active-recall" main.js | grep "^0$" && gh release view 1.0.0 --json assets --jq '.assets | length'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -c "active-recall" main.js` returns 0 (no old names in compiled output)
    - `grep "self-test" main.js` returns at least one match
    - `gh release view 1.0.0 --json assets --jq '.assets[].name'` lists main.js, manifest.json, styles.css (3 assets)
    - `gh release view 1.0.0 --json tagName --jq '.tagName'` returns `1.0.0` (no v prefix, per D-15)
  </acceptance_criteria>
  <done>Production build is clean with renamed identifiers. GitHub release 1.0.0 has three updated assets attached.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify renamed plugin in Obsidian</name>
  <files></files>
  <action>Present the human-verify checkpoint to the user. The automated work (rename + build + release update) is complete. Now the user needs to verify the plugin works correctly in Obsidian with the new name.</action>
  <what-built>Complete plugin rename from "AI Active Recall" to "Self Test" with updated README, production build, and GitHub release.</what-built>
  <how-to-verify>
    1. Reload the plugin in Obsidian (Settings > Community Plugins > disable then enable, or restart Obsidian)
    2. If the sidebar pane doesn't appear after reload, open it via command palette: "Open Self Test Panel" (the old workspace leaf type is now invalid, which is expected per D-06)
    3. Verify these items:
       - Settings > Community Plugins shows "Self Test" (not "AI Active Recall")
       - Settings tab is labeled "Self Test"
       - Command palette shows "Open Self Test Panel" (not "Open Active Recall Panel")
       - Sidebar header says "Self Test"
       - All four sidebar tabs work (Folders, Tags, Links, Notes)
       - Generate a self-test from any mode to confirm end-to-end works
       - README.md reads clearly for a new user
    4. If anything shows the old "Active Recall" or "AI Active Recall" name, describe what and where.
  </how-to-verify>
  <verify>Human confirms all verification items pass</verify>
  <done>User has approved the renamed plugin works correctly in Obsidian.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Submit to Obsidian community store</name>
  <files></files>
  <read_first>manifest.json, .planning/phases/12-v2-release/12-RESEARCH.md</read_first>
  <action>
Before opening the PR, re-read the current submission requirements:
- Check https://github.com/obsidianmd/obsidian-releases for the latest PR template and community-plugins.json format
- Use `gh api repos/obsidianmd/obsidian-releases/contents/community-plugins.json --jq '.content' | base64 -d | tail -20` to see the format of recent entries

Steps:
1. Fork obsidianmd/obsidian-releases (if not already forked): `gh repo fork obsidianmd/obsidian-releases --clone=false`
2. Clone or sync the fork
3. Add the plugin entry to `community-plugins.json` in alphabetical order by id. The entry:
```json
{
  "id": "self-test",
  "name": "Self Test",
  "author": "Agustin Gabrielli",
  "description": "Generate self-testing questions from your notes using AI to practice active recall.",
  "repo": "agus-gabrielli/obsidian-active-recall"
}
```
The `description` field MUST exactly match manifest.json (the automated bot validates this).
4. Create a PR against obsidianmd/obsidian-releases with:
   - Title: "Add Self Test plugin"
   - Body should include the PR checklist items from the template:
     - Plugin has been tested and works
     - GitHub release contains main.js, manifest.json, styles.css
     - Release tag matches manifest version (1.0.0)
     - README describes plugin purpose and usage
     - LICENSE file is present

Note: The exact PR creation mechanics depend on the current obsidianmd/obsidian-releases PR template. Read the template first and follow it exactly. If the process requires steps not covered here, adapt accordingly.
  </action>
  <verify>
    <automated>gh pr list --repo obsidianmd/obsidian-releases --author @me --state open --json title --jq '.[0].title'</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr list --repo obsidianmd/obsidian-releases --author @me --state open` shows at least one open PR
    - The PR title contains "Self Test"
    - The community-plugins.json entry has `"id": "self-test"` matching manifest.json exactly
    - The PR description includes the submission checklist items
  </acceptance_criteria>
  <done>PR is open against obsidianmd/obsidian-releases adding the "Self Test" plugin entry. The automated bot will validate the entry against the GitHub release and manifest.</done>
</task>

</tasks>

<verification>
- Production build contains no old names
- GitHub release 1.0.0 has 3 updated assets
- Plugin loads as "Self Test" in Obsidian (human verified)
- PR is open against obsidianmd/obsidian-releases
</verification>

<success_criteria>
- The plugin is renamed, built, released, and submitted to the Obsidian community store
- Human has verified the renamed plugin works correctly in Obsidian
</success_criteria>

<output>
After completion, create `.planning/phases/12-v2-release/12-03-SUMMARY.md`
</output>
